'use client';

/**
 * Placeholder content for "Charts" tab.
 * UI only; real charts to be wired up later.
 */
export default function ChartsPlaceholder({ message = 'Charts coming soon.' }) {
  return (
    <div className="card bg-base-200 rounded-box p-8 text-center">
      <p className="text-base-content/70">{message}</p>
    </div>
  );
}
